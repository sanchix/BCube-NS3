  NodeContainer nodos(equipos);

  NodeContainer Bcube0(NodeContainer &nodos){
    uint32 Nequipos = nodos.GetN();
    double niveles = pow(Nequipos,1)
    NodeContainer switches;
    
    return switches;
  }

  NodeContainer Bcube1(NodeContainer &nodos){
    uint32 Nequipos = nodos.GetN();
    double niveles = pow(Nequipos,0.5)
    NodeContainer switches;
    
    return switches;
  }  

  NodeContainer Bcube2(NodeContainer &nodos){
    uint32 Nequipos = nodos.GetN();
    double niveles = pow(Nequipos,0.5)
    NodeContainer switches;

    return switches;
  }

hola que tal estamos huevo